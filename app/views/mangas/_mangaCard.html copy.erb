

<div class='container'>
	<div class="container-fluid mt-2">
		<div class="row m-2 card-all">
			<% @mangas.each do |manga|%>
					
		<article class="card-last-manga card--1">
			<div class="card__img" style="background-image: url('<%= manga.image_url %>')"></div>
			<a href="#" class="card_link">
			<div class="card__img--hover" style="background-image: url('<%= manga.image_url %>')"></div>
			</a>
			<div class="card__info">
			<h3 class="card__title"><% if @all_libraries.include?(manga.id) %><%= image_tag 'available.png', class:"available mb-1"%><% end %> <%= manga.title.capitalize %></h3>
				<% manga.categories.each do |category| %>
				<span><small><%= link_to category.name, category_path(category.id) %></small></span>
				<%end%><br/>
			<span class="card__by">by <%= manga.author%></span><br>
			<%= link_to "Voir le manga", manga_path(manga), class: 'btn btn-info mt-2'%>
			</div>
		</article>
					
			<% end %>
		</div>
		<%= pagy_bootstrap_nav(@pagy).html_safe%>
	</div>
</div>

